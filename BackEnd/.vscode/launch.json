{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Debugar Aplicação",
      "preLaunchTask": "",
      "program": "${workspaceFolder}/src/server/server.ts",
      "outFiles": ["${workspaceFolder}/dist/**/*.js"]
    }
  ]
}